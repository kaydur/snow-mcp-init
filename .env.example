# ServiceNow MCP Server Configuration

# ServiceNow instance URL (required)
SERVICENOW_INSTANCE_URL=https://your-instance.service-now.com

# ServiceNow credentials (required)
SERVICENOW_USERNAME=your-username
SERVICENOW_PASSWORD=your-password

# Logging level (optional, default: info)
# Options: debug, info, warn, error
LOG_LEVEL=info

# Reject unauthorized SSL certificates (optional, default: true)
# Set to false for development instances with self-signed certificates
# WARNING: Only use false in development environments
REJECT_UNAUTHORIZED=true
